# 🚀 部署指南 - 台灣 50 模擬交易平台

本指南將帶您一步步完成專案部署，讓您的應用程式上線使用。

## 📋 部署前準備

### 1. 確認專案可以正常建置

在部署前，請先在本地測試建置：

```bash
# 安裝依賴（如果還沒安裝）
npm install

# 建置專案
npm run build

# 預覽建置結果（選填）
npm run preview
```

如果建置成功，您會看到 `dist/` 資料夾，裡面包含所有要部署的檔案。

### 2. 準備 GitHub Repository（推薦）

如果您還沒有將程式碼推送到 GitHub：

1. 在 GitHub 創建新的 repository
2. 初始化 git（如果還沒有）：
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```
3. 連接並推送到 GitHub：
   ```bash
   git remote add origin https://github.com/你的用戶名/你的專案名稱.git
   git branch -M main
   git push -u origin main
   ```

---

## 🌐 方式一：Vercel 部署（最推薦 ⭐）

Vercel 是最簡單快速的部署方式，完全免費且自動提供 HTTPS。

### 步驟：

#### 方法 A：透過 GitHub（推薦）

1. **前往 Vercel**
   - 打開 https://vercel.com
   - 使用 GitHub 帳號登入

2. **匯入專案**
   - 點擊 "Add New..." → "Project"
   - 選擇您的 GitHub repository
   - Vercel 會自動偵測這是 Vite 專案

3. **設定專案**
   - **Framework Preset**: Vite（應該已自動偵測）
   - **Root Directory**: `./`（預設即可）
   - **Build Command**: `npm run build`（預設即可）
   - **Output Directory**: `dist`（預設即可）

4. **部署**
   - 點擊 "Deploy"
   - 等待 1-2 分鐘建置完成
   - 完成後會提供一個網址，例如：`https://your-project.vercel.app`

5. **完成！**
   - 您的網站已經上線了！
   - 之後每次推送到 GitHub，Vercel 會自動重新部署

#### 方法 B：透過 Vercel CLI

1. **安裝 Vercel CLI**
   ```bash
   npm install -g vercel
   ```

2. **登入 Vercel**
   ```bash
   vercel login
   ```

3. **部署**
   ```bash
   vercel
   ```
   按照提示操作即可。

### 優點：
- ✅ 完全免費
- ✅ 自動 HTTPS
- ✅ 全球 CDN 加速
- ✅ 自動部署（Git push 後自動更新）
- ✅ 零配置

---

## 🌐 方式二：Netlify 部署

Netlify 也是很好的選擇，操作簡單。

### 步驟：

#### 方法 A：透過 GitHub（推薦）

1. **前往 Netlify**
   - 打開 https://www.netlify.com
   - 使用 GitHub 帳號登入

2. **匯入專案**
   - 點擊 "Add new site" → "Import an existing project"
   - 選擇 "Deploy with GitHub"
   - 選擇您的 repository

3. **設定建置**
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
   - Netlify 會自動讀取 `netlify.toml` 設定檔

4. **部署**
   - 點擊 "Deploy site"
   - 等待建置完成
   - 完成後會提供一個網址，例如：`https://your-project.netlify.app`

5. **完成！**
   - 網站已上線
   - 之後每次推送到 GitHub，Netlify 會自動重新部署

#### 方法 B：拖放部署

1. **建置專案**
   ```bash
   npm run build
   ```

2. **拖放部署**
   - 前往 https://app.netlify.com/drop
   - 直接將 `dist` 資料夾拖放到頁面上
   - 立即部署完成

### 優點：
- ✅ 完全免費
- ✅ 自動 HTTPS
- ✅ 全球 CDN
- ✅ 自動部署

---

## 🌐 方式三：GitHub Pages 部署

適合想要使用免費 GitHub Pages 的用戶。

### 步驟：

1. **安裝 gh-pages**
   ```bash
   npm install --save-dev gh-pages
   ```

2. **修改 vite.config.ts**
   
   在 `vite.config.ts` 中添加 `base` 設定：
   ```typescript
   export default defineConfig(({ mode }) => {
     const env = loadEnv(mode, '.', '');
     const isProduction = mode === 'production';
     
     return {
       base: mode === 'production' ? '/你的-repository-名稱/' : '/',
       // ... 其他設定
     };
   });
   ```
   
   > ⚠️ 將 `你的-repository-名稱` 改為您的 GitHub repository 名稱

3. **新增部署腳本到 package.json**
   
   在 `package.json` 的 `scripts` 中添加：
   ```json
   "scripts": {
     "deploy": "npm run build && gh-pages -d dist"
   }
   ```

4. **執行部署**
   ```bash
   npm run deploy
   ```

5. **啟用 GitHub Pages**
   - 前往您的 GitHub repository
   - Settings → Pages
   - Source: 選擇 `gh-pages` branch
   - 點擊 Save

6. **完成！**
   - 您的網站會在 `https://你的用戶名.github.io/你的-repository-名稱/` 上線

### 優點：
- ✅ 完全免費
- ✅ 與 GitHub 整合
- ⚠️ 需要手動更新 base URL

---

## 🐳 方式四：Docker 部署

適合想要部署到自己的伺服器或雲端平台（如 AWS、Google Cloud、Azure）。

### 步驟：

1. **建置 Docker 映像**
   ```bash
   docker build -t tw50-simulator .
   ```

2. **執行容器**
   ```bash
   docker run -d -p 80:80 --name tw50-simulator tw50-simulator
   ```

3. **訪問網站**
   - 打開瀏覽器訪問 `http://localhost`
   - 如果部署到雲端，訪問伺服器的 IP 或域名

### 部署到雲端平台：

#### AWS / Google Cloud / Azure

1. 將 Docker 映像推送到容器註冊表（如 Docker Hub）
2. 在雲端平台創建容器實例
3. 設定端口映射和網路設定
4. 啟動容器

---

## 📱 方式五：傳統伺服器部署

如果您有自己的 VPS 或伺服器。

### 步驟：

1. **建置專案**
   ```bash
   npm run build
   ```

2. **上傳檔案**
   - 將 `dist/` 資料夾中的所有檔案上傳到伺服器的 web 根目錄
   - 例如：`/var/www/html` 或 `/usr/share/nginx/html`

3. **設定 Web 伺服器**

   **Nginx 設定範例**（`/etc/nginx/sites-available/default`）：
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;
       root /var/www/html;
       index index.html;

       location / {
           try_files $uri $uri/ /index.html;
       }

       location /assets {
           expires 1y;
           add_header Cache-Control "public, immutable";
       }
   }
   ```

   **Apache 設定範例**（`.htaccess`）：
   ```apache
   <IfModule mod_rewrite.c>
     RewriteEngine On
     RewriteBase /
     RewriteRule ^index\.html$ - [L]
     RewriteCond %{REQUEST_FILENAME} !-f
     RewriteCond %{REQUEST_FILENAME} !-d
     RewriteRule . /index.html [L]
   </IfModule>
   ```

4. **重啟 Web 伺服器**
   ```bash
   # Nginx
   sudo systemctl restart nginx
   
   # Apache
   sudo systemctl restart apache2
   ```

---

## ✅ 部署後檢查清單

部署完成後，請確認：

- [ ] 網站可以正常訪問
- [ ] 所有頁面路由正常運作（刷新頁面不會 404）
- [ ] 股票數據可以正常載入
- [ ] 交易功能正常運作
- [ ] 在手機上測試響應式設計
- [ ] 檢查瀏覽器控制台是否有錯誤

---

## 🐛 常見問題

### Q1: 部署後頁面顯示空白？

**A:** 檢查以下項目：
- 確認 `base` URL 設定正確（GitHub Pages 需要設定）
- 確認所有路由都正確指向 `index.html`
- 檢查瀏覽器控制台的錯誤訊息

### Q2: 路由無法正常運作（刷新後 404）？

**A:** 這是 SPA（單頁應用）的常見問題，需要設定 rewrite 規則：
- **Vercel/Netlify**: 已自動處理（透過設定檔）
- **Nginx**: 確認有 `try_files $uri $uri/ /index.html;`
- **Apache**: 確認 `.htaccess` 設定正確

### Q3: 股票數據無法載入？

**A:** 可能是 CORS 問題：
- 檢查網路連線
- 確認 TWSE API 可正常存取
- 檢查瀏覽器控制台錯誤

### Q4: 建置失敗？

**A:** 
- 確認 Node.js 版本為 18+
- 刪除 `node_modules` 重新安裝：`rm -rf node_modules && npm install`
- 檢查錯誤訊息

---

## 🎉 完成！

恭喜！您的台灣 50 模擬交易平台已經成功部署上線了！

### 推薦部署方式排序：

1. **Vercel** ⭐⭐⭐⭐⭐ - 最簡單快速
2. **Netlify** ⭐⭐⭐⭐⭐ - 也很簡單
3. **GitHub Pages** ⭐⭐⭐⭐ - 免費但需要設定
4. **Docker** ⭐⭐⭐ - 適合進階用戶
5. **傳統伺服器** ⭐⭐⭐ - 需要自己的伺服器

選擇最適合您的方式開始部署吧！

---

## 📞 需要幫助？

如果遇到問題，請：
1. 檢查瀏覽器控制台的錯誤訊息
2. 查看部署平台的建置日誌
3. 參考專案的 `DEPLOYMENT.md` 文件
4. 提交 Issue 到 GitHub repository
